version: '3'

services:
  specmatic:
    image: znsio/specmatic:2.0.7
    command: stub
    environment:
      - SPECMATIC_GENERATIVE_STUB=true
    volumes:
      - ./specmatic.yaml:/usr/src/app/specmatic.yaml
    ports:
      - "9000"

  nginx:
    image: nginx:alpine
    ports:
      - "8621:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - specmatic